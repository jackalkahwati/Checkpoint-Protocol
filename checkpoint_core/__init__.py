"""Checkpoint Core Protocol: a Git-replacement version-control protocol for
human + AI-generated code. Sessions, snapshots, diffs, verification, accepts,
branches, merges, and sync are native objects. Git is only an import/export bridge.

If Git disappeared, Checkpoint Core would still work.
"""

__version__ = "0.1.0"
SCHEMA_VERSION = 1
